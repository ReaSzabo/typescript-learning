// Create a function that has 2 parameters.
// a and b
// The function should return the difference of the 2 numbers.
// (difference = különbség)

function difi(a: number, b: number): number {
    return a - b;
}